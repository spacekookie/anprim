//! A file that contains some simple unit tests because OMG we do those now

#[cfg(test)]
mod test {

    #[test]
    fn css_parser() {

        // Good that we talked about it
        assert!(true == true);
    }

    #[test]
    fn html_parser() {
        assert!(true == true);
    }
}